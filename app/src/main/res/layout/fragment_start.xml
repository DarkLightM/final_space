<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.Start">

    <ImageView
        android:id="@+id/main_picture"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/main_page">
    </ImageView>
    <androidx.constraintlayout.widget.ConstraintLayout
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <Button
            android:id="@+id/characters_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_width="@dimen/main_activity_button"
            android:layout_height="wrap_content"
            android:text="@string/characters_name">
        </Button>
        <Button
            android:id="@+id/episodes_button"
            app:layout_constraintTop_toBottomOf="@id/characters_button"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_width="@dimen/main_activity_button"
            android:layout_height="wrap_content"
            android:text="@string/episodes_name">
        </Button>
        <Button
            android:id="@+id/locations_button"
            app:layout_constraintTop_toBottomOf="@id/episodes_button"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_width="@dimen/main_activity_button"
            android:layout_height="wrap_content"
            android:text="@string/locations_name">
        </Button>
        <Button
            android:id="@+id/quotes_button"
            app:layout_constraintTop_toBottomOf="@id/locations_button"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_width="@dimen/main_activity_button"
            android:layout_height="wrap_content"
            android:text="@string/quotes_name">
        </Button>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>